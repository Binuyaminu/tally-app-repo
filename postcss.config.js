module.exports = {
    plugins: {
        tailwindcss: {}, // This tells PostCSS to use the Tailwind CSS plugin
        autoprefixer: {}, // This ensures CSS prefixes are added for browser compatibility
    },
};